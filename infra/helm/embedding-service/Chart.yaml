apiVersion: v2
name: embedding-service
description: Embedding service for vector ingestion pipeline - loads sentence-transformers model and upserts embeddings to Qdrant
type: application
version: 1.0.0
appVersion: "1.0.0"
