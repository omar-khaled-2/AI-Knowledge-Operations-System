apiVersion: v1
kind: Service
metadata:
  name: document-processor
  labels:
    app: document-processor
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: document-processor
