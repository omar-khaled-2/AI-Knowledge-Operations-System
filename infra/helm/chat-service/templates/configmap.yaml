apiVersion: v1
kind: ConfigMap
metadata:
  name: chat-service-config
  labels:
    app.kubernetes.io/name: chat-service
data:
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  CHUNK_DELAY_MS: {{ .Values.config.chunkDelayMs | quote }}
